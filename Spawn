-- Services
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- Safely resolve CoreGui or fallback to PlayerGui
local guiParent = pcall(function() return CoreGui.Name end) and CoreGui or playerGui

-- Cleanup any previous instances
if guiParent:FindFirstChild("SecureLoaderGui") then
    guiParent.SecureLoaderGui:Destroy()
end

---------------------------------------------------------
-- SECURE LOADER SCREEN (Full UI Setup)
---------------------------------------------------------
local loaderGui = Instance.new("ScreenGui")
loaderGui.Name = "SecureLoaderGui"
loaderGui.ResetOnSpawn = false
loaderGui.Parent = guiParent

local loaderFrame = Instance.new("Frame")
loaderFrame.Name = "MainFrame"
loaderFrame.Size = UDim2.new(0, 480, 0, 240)
loaderFrame.Position = UDim2.new(0.5, -240, 0.5, -120)
loaderFrame.BackgroundColor3 = Color3.fromRGB(15, 18, 28)
loaderFrame.BorderSizePixel = 0
loaderFrame.Active = true
loaderFrame.Draggable = true
loaderFrame.ClipsDescendants = true
loaderFrame.Parent = loaderGui

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 16)
mainCorner.Parent = loaderFrame

local mainStroke = Instance.new("UIStroke")
mainStroke.Color = Color3.fromRGB(45, 55, 80)
mainStroke.Thickness = 1.5
mainStroke.Parent = loaderFrame

local titleLabel = Instance.new("TextLabel")
titleLabel.Size = UDim2.new(0, 180, 0, 30)
titleLabel.Position = UDim2.new(0, 24, 0, 18)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "Blox Fruits  <font color='rgb(80,240,140)'>•</font>"
titleLabel.RichText = true
titleLabel.TextColor3 = Color3.fromRGB(140, 170, 240)
titleLabel.TextSize = 17
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.Parent = loaderFrame

local loaderLabel = Instance.new("TextLabel")
loaderLabel.Size = UDim2.new(0, 160, 0, 30)
loaderLabel.Position = UDim2.new(1, -184, 0, 18)
loaderLabel.BackgroundTransparency = 1
loaderLabel.Text = "🛡️ SECURE LOADER"
loaderLabel.TextColor3 = Color3.fromRGB(100, 130, 200)
loaderLabel.TextSize = 13
loaderLabel.Font = Enum.Font.GothamBold
loaderLabel.TextXAlignment = Enum.TextXAlignment.Right
loaderLabel.Parent = loaderFrame

local percentLabel = Instance.new("TextLabel")
percentLabel.Size = UDim2.new(1, 0, 0, 60)
percentLabel.Position = UDim2.new(0, 0, 0.28, 0)
percentLabel.BackgroundTransparency = 1
percentLabel.Text = "0.0%"
percentLabel.TextColor3 = Color3.fromRGB(215, 235, 255)
percentLabel.TextSize = 52
percentLabel.Font = Enum.Font.GothamBold
percentLabel.Parent = loaderFrame

local trackFrame = Instance.new("Frame")
trackFrame.Size = UDim2.new(0.9, 0, 0, 6)
trackFrame.Position = UDim2.new(0.05, 0, 0.64, 0)
trackFrame.BackgroundColor3 = Color3.fromRGB(28, 35, 52)
trackFrame.BorderSizePixel = 0
trackFrame.Parent = loaderFrame

local trackCorner = Instance.new("UICorner")
trackCorner.CornerRadius = UDim.new(1, 0)
trackCorner.Parent = trackFrame

local fillFrame = Instance.new("Frame")
fillFrame.Size = UDim2.new(0, 0, 1, 0)
fillFrame.BackgroundColor3 = Color3.fromRGB(75, 135, 255)
fillFrame.BorderSizePixel = 0
fillFrame.Parent = trackFrame

local fillCorner = Instance.new("UICorner")
fillCorner.CornerRadius = UDim.new(1, 0)
fillCorner.Parent = fillFrame

local knob = Instance.new("Frame")
knob.Size = UDim2.new(0, 14, 0, 14)
knob.Position = UDim2.new(1, -7, 0.5, -7)
knob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
knob.BorderSizePixel = 0
knob.Parent = fillFrame

local knobCorner = Instance.new("UICorner")
knobCorner.CornerRadius = UDim.new(1, 0)
knobCorner.Parent = knob

local knobStroke = Instance.new("UIStroke")
knobStroke.Color = Color3.fromRGB(120, 180, 255)
knobStroke.Thickness = 2
knobStroke.Parent = knob

local statusLabel = Instance.new("TextLabel")
statusLabel.Size = UDim2.new(1, 0, 0, 20)
statusLabel.Position = UDim2.new(0, 0, 0.74, 0)
statusLabel.BackgroundTransparency = 1
statusLabel.Text = "initializing game context..."
statusLabel.TextColor3 = Color3.fromRGB(90, 115, 150)
statusLabel.TextSize = 13
statusLabel.Font = Enum.Font.Code
statusLabel.Parent = loaderFrame

local subLabel = Instance.new("TextLabel")
subLabel.Size = UDim2.new(1, 0, 0, 20)
subLabel.Position = UDim2.new(0, 0, 0.85, 0)
subLabel.BackgroundTransparency = 1
subLabel.Text = "First load may take longer than usual."
subLabel.TextColor3 = Color3.fromRGB(60, 75, 100)
subLabel.TextSize = 12
subLabel.Font = Enum.Font.Gotham
subLabel.Parent = loaderFrame

-- 1-Minute Loading Logic (60 seconds)
local totalDuration = 60
local startTime = tick()

local progressTween = TweenService:Create(
    fillFrame, 
    TweenInfo.new(totalDuration, Enum.EasingStyle.Linear), 
    {Size = UDim2.new(1, 0, 1, 0)}
)
progressTween:Play()

task.spawn(function()
    while true do
        local elapsed = tick() - startTime
        local progress = math.clamp(elapsed / totalDuration, 0, 1)
        local percentValue = progress * 100
        
        percentLabel.Text = string.format("%.1f%%", percentValue)
        
        if percentValue >= 85 then
            statusLabel.Text = "finalizing injection & security checks..."
        elseif percentValue >= 60 then
            statusLabel.Text = "decrypting script payload..."
        elseif percentValue >= 35 then
            statusLabel.Text = "bypassing security checks..."
        elseif percentValue >= 15 then
            statusLabel.Text = "fetching remote assets..."
        end
        
        if progress >= 1 then
            percentLabel.Text = "100.0%"
            statusLabel.Text = "successfully loaded!"
            statusLabel.TextColor3 = Color3.fromRGB(90, 230, 140)
            
            task.wait(0.8)
            
            -- Smooth fade out animation
            local fadeInfo = TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out)
            local fadeOut = TweenService:Create(loaderFrame, fadeInfo, {
                Position = UDim2.new(0.5, -240, 0.5, -140),
                BackgroundTransparency = 1
            })
            
            for _, child in ipairs(loaderFrame:GetDescendants()) do
                if child:IsA("TextLabel") then
                    TweenService:Create(child, fadeInfo, {TextTransparency = 1}):Play()
                elseif child:IsA("Frame") then
                    TweenService:Create(child, fadeInfo, {BackgroundTransparency = 1}):Play()
                elseif child:IsA("UIStroke") then
                    TweenService:Create(child, fadeInfo, {Transparency = 1}):Play()
                end
            end
            
            fadeOut:Play()
            fadeOut.Completed:Wait()
            
            loaderGui:Destroy()
            
            -- Fetch and execute your main script from GitHub
            local targetUrl = "https://raw.githubusercontent.com/jamiulhasanjishan-maker/Bloxfruit-/refs/heads/main/Spawn"
            local success, payload = pcall(function()
                return game:HttpGet(targetUrl, true)
            end)
            
            if success and payload and #payload > 0 then
                local executeFunc, compileError = loadstring(payload)
                if executeFunc then
                    task.spawn(executeFunc)
                else
                    warn("[Loader Error] Failed to compile payload: " .. tostring(compileError))
                end
            else
                warn("[Loader Error] Failed to fetch script: " .. tostring(payload))
            end
            
            break
        end
        
        task.wait(0.04)
    end
end)
